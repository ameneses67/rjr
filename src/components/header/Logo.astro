---
// Librerías
import { twMerge } from "tailwind-merge";
interface Props {
  classes?: string;
}

const { classes } = Astro.props;
---

<a id="logo" class={twMerge("dark:text-white z-10", classes)} href="/"
  ><span id="logoSpan" class="font-bold text-skin-accent">Retirate</span
  >JovenRico</a
>
